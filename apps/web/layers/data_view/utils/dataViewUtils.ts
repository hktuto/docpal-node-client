/**
 * Utility functions for data_view layer
 */

export const dataViewUtils = {
  /**
   * Format data for display
   */
  formatData: (data: any) => {
    return data
  },
  
  /**
   * Validate input
   */
  validate: (input: string): boolean => {
    return input.length > 0
  }
}
